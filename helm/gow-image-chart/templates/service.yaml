apiVersion: v1
kind: Service
metadata:
  dndndname: {{ include "gow-image-chart.fullname" . }}
  labels:
    ddd{{- include "gow-image-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gow-image-chart.selectorLabels" . | nindent 4 }}
